export default function ChartContainer({
     chartInfo
}: {
    chartInfo: {
        name: string;
        typeChart: string;
    };
}) {
    return (
        <div>
            <h1>Chart Container</h1>
        </div>  
    );
}